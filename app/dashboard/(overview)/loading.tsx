import DashboardSkeleton from '@/app/ui/skeletons';

export default function Loading() {
  return <DashboardSkeleton />;
}

// Regla de next, cualquier archivo loading se va a usar como predeterminado hasta la llegada de datos (que)
