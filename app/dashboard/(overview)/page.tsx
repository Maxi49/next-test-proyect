import Loading from './loading';

export default function OverViewPage() {
  return <Loading />;
}

/**
 * carpeta overview:
 * Lo que permite el overview es que se aplique un componente unicamente a la rama principal, en este caso dashboard.
 * Entonces esto no se aplicaria ni a customers ni a invoices 
 */
